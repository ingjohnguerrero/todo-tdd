version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"

  run-unit-tests:
    macos:
      xcode: "15.0.0"  # Updated to Xcode 15
    steps:
      - checkout
      - run: 
          name: Resolve Swift Package Manager dependencies for ToDoTests
          command: xcodebuild -resolvePackageDependencies -scheme ToDoTests -workspace ToDo.xcodeproj/project.xcworkspace
      - run:
          name: Run Unit Tests for ToDoTests
          command: xcodebuild test -scheme ToDoTests -workspace ToDo.xcodeproj/project.xcworkspace -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 13,OS=latest'

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - run-unit-tests
